---
layout: layout.njk
title: Partenariat - Oval Saône
description: Devenez partenaire de l'École de Rugby Oval Saône. Soutenez le rugby local à Trévoux et bénéficiez d'avantages fiscaux et d'une visibilité unique. Entreprises et particuliers bienvenus.
---
<!-- Hero Section avec effet parallax -->
<section class="hero parallax-section subpage-hero" data-parallax-speed="0.25">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">{{ page_partenariat.hero_title  }}</h1>
        <p class="hero-subtitle">{{ page_partenariat.hero_subtitle  }}</p>
    </div>
</section>

<!-- Sponsors -->
<section class="sponsors-section section intro">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>{{ page_partenariat.sponsor_title }}</h2>
                <p>{{ page_partenariat.sponsor_subtitle }}</p>
            </div>
        </div>
        <div class="sponsors-list" id="sponsors-list">
            <!-- Les sponsors seront chargés dynamiquement -->
            {% for sponsor in sponsors %}
                <div class="sponsor-card fade-in-up">
                {% if sponsor.logo %}
                <div class="sponsor-logo">
                    <img src="{{ sponsor.logo }}" alt="{{ sponsor.nom }}" onerror="this.src='assets/logo.png'">                
                </div>
                {% endif %}
                <div class="sponsor-info">
                    <h3 class="sponsor-name">{{ sponsor.nom }}</h3>
                    <p class="sponsor-description">{{ sponsor.description }}</p>
                    <div class="sponsor-details">
                        {% if sponsor.website %}<a href="{{ sponsor.website }}" target="_blank" class="sponsor-link">🌐 Site web</a>{% endif %}
                        {% if sponsor.telephone %}<span class="sponsor-phone">📞 {{ sponsor.telephone }}</span>{% endif %}
                        {% comment %} {% if sponsor.email %}<a href="mailto:{{ sponsor.email }}" class="sponsor-email">✉️ Email</a>{% endif %} {% endcomment %}
                    </div>
                    <div class="sponsor-partenariat">
                        Partenaire depuis {{ sponsor.partenariat_depuis }}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        <div class="dons-actions">
            <a href="./contact.html" class="btn btn-primary btn-large">
                <i class="fas fa-handshake"></i>
                {{ page_partenariat.cta_sponsor_button }}
            </a>
        </div>
    </div>
</section>

<!-- Fonctionnement du sponsoring -->
<section class="partenariat-section section bg-light">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>{{ page_partenariat.partenariat_title }}</h2>
                <p>{{ page_partenariat.partenariat_description }}</p>
            </div>
        </div>
        <div class="content-grid">
            <div class="content-text">
                <div class="cta-partenariat" id="cta-partenariat">
                    <div class="cta-header">
                        <h4 class="cta-sous-titre">{{ page_partenariat.partenariat_entreprise.subtitle }}</h4>
                        <p class="cta-description">{{ page_partenariat.partenariat_entreprise.description }}</p>
                    </div>

                    <div class="avantages-grid">
                        {% for advantage in page_partenariat.partenariat_entreprise.advantages %}
                            <div class="avantage-item">
                                <div class="avantage-icone">{{ advantage.icone }}</div>
                                <div class="avantage-content">
                                    <h5>{{ advantage.titre }}</h5>
                                    <p>{{ advantage.description }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="content-image">
                <img src="/assets/sponsoring.jpg" alt="Devenir partenaire">
            </div>
        </div>
        <div class="dons-actions">
            <a href="./contact.html" class="btn btn-primary btn-large">
                <i class="fas fa-handshake"></i>
                {{ page_partenariat.cta_sponsor_button }}
            </a>
        </div>
    </div>
</section>

<!-- Section pour les particuliers -->
<section class="particuliers-section section bg-light">
    <div class="container">
        <div class="content-grid">
            <div class="content-image">
                <img src="/assets/sponsoring2.jpg" alt="Soutenir le club">
            </div>
            <div class="content-text">
                <div class="cta-particuliers" id="cta-particuliers">
                    <div class="cta-header">
                        <h4 class="cta-sous-titre">{{ page_partenariat.partenariat_particulier.subtitle }}</h4>
                        <p class="cta-description">{{ page_partenariat.partenariat_particulier.description }}</p>
                    </div>
                    
                    <div class="avantages-grid">
                        {% for advantage in page_partenariat.partenariat_particulier.advantages %}
                            <div class="avantage-item">
                                <div class="avantage-icone">{{ advantage.icone }}</div>
                                <div class="avantage-content">
                                    <h5>{{ advantage.titre }}</h5>
                                    <p>{{ advantage.description }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
        {% comment %} <div class="paliers-section" id="paliers-dons">
            <!-- Les paliers de dons seront chargés dynamiquement -->
        </div> {% endcomment %}
        <div class="dons-actions">
            <a href="https://www.helloasso.com" target="_blank" class="btn btn-primary btn-large">
                <i class="fas fa-heart"></i>
                Faire un don via HelloAsso
            </a>
        </div>
    </div>
</section>

<!-- Section Avantages Fiscaux -->
<section class="fiscalite-section section">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>Avantages fiscaux</h2>
                <p>En tant que partenaire ou donateur, vous pouvez bénéficier d'avantages fiscaux intéressants. Découvrez comment votre soutien peut être avantageux pour vous.</p>
            </div>
        </div>
        <div id="avantages-fiscaux" class="avantages-fiscaux">
            <div class="avantages-fiscaux-content">
                <h3>Avantages Fiscaux</h3>
                <div class="avantages-grid">
                    <div class="avantage-fiscal">
                        <h4>🏢 Pour les Entreprises</h4>
                        <p>{{ page_partenariat.partenariat_entreprise.fiscal_advantages }}</p>
                    </div>
                    <div class="avantage-fiscal">
                        <h4>👤 Pour les Particuliers</h4>
                        <p>{{ page_partenariat.partenariat_particulier.fiscal_advantages }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
