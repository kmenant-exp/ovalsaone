---
layout: layout.njk
title: "Rugby pour Enfants à Trévoux - Initiation, Formation et Compétition"
description: "Guide complet sur le rugby pour enfants à Trévoux. Découvrez les bienfaits du rugby, nos méthodes d'apprentissage, le développement moteur et les valeurs transmises aux U4-U14."
---

<!-- Section Hero -->
<section class="subpage-hero parallax-section" data-parallax-speed="0.3">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">{{ page_rugby_enfants.hero.title }}</h1>
        <p class="hero-subtitle">{{ page_rugby_enfants.hero.subtitle }}</p>
    </div>
</section>

<section class="intro section">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>{{ page_rugby_enfants.intro.title }}</h2>
                <p>{{ page_rugby_enfants.intro.text }}</p>
            </div>
        </div>
    </div>
</section>

<!-- Contenu principal -->
<main class="container rugby-enfants-page">
    <article>

        {% comment %} <!-- Chiffres clés -->
        <div class="re-stats-grid">
            <div class="re-stat-card">
                <span class="re-stat-number">4+</span>
                <span class="re-stat-label">Ans – dès la maternelle</span>
            </div>
            <div class="re-stat-card">
                <span class="re-stat-number">6</span>
                <span class="re-stat-label">Catégories d'âge</span>
            </div>
            <div class="re-stat-card">
                <span class="re-stat-number">15</span>
                <span class="re-stat-label">Enfants max / groupe</span>
            </div>
            <div class="re-stat-card">
                <span class="re-stat-number">100%</span>
                <span class="re-stat-label">Éducateurs diplômés</span>
            </div>
        </div> {% endcomment %}

        {% for section in page_rugby_enfants.sections %}
        <section class="re-section{% if section.title contains 'sécurité' %} re-section--security{% endif %}{% if section.title contains 'environnement' %} re-section--location{% endif %}">
            <h2>{{ section.title }}</h2>
            {% if section.intro %}
            <p>{{ section.intro }}</p>
            {% endif %}
            
            {% if section.items %}
            <ul class="re-list">
                {% for item in section.items %}
                <li>{{ item }}</li>
                {% endfor %}
            </ul>
            {% endif %}
            
            {% if section.subsections %}

            {% comment %}Image avant la section méthode d'apprentissage{% endcomment %}
            {% if section.title contains 'méthode' %}
            <div class="re-image-banner">
                <img src="/assets/rugby-enfants/fond2.webp" 
                     alt="Entraînement de rugby pour les jeunes" 
                     loading="lazy" width="1200" height="300">
            </div>
            {% endif %}

            {% for subsection in section.subsections %}
                {% if section.title contains 'méthode' %}
                <div class="re-age-card">
                    <h3>
                        {% if subsection.subtitle contains 'U4' %}<span class="re-age-badge">4-6 ans</span>
                        {% elsif subsection.subtitle contains 'U8' %}<span class="re-age-badge">7-10 ans</span>
                        {% elsif subsection.subtitle contains 'U12' %}<span class="re-age-badge">11-14 ans</span>
                        {% endif %}
                        {{ subsection.subtitle }}
                    </h3>
                    {% if subsection.content %}<p>{{ subsection.content }}</p>{% endif %}
                    {% if subsection.items %}
                    <ul class="re-list">
                        {% for item in subsection.items %}<li>{{ item }}</li>{% endfor %}
                    </ul>
                    {% endif %}
                </div>
                {% else %}
                <div class="re-subsection">
                    <h3>
                        {% if subsection.subtitle contains 'bienfaits' %}<span class="re-icon">💪</span>
                        {% elsif subsection.subtitle contains 'valeurs' %}<span class="re-icon">🤝</span>
                        {% endif %}
                        {{ subsection.subtitle }}
                    </h3>
                    {% if subsection.content %}<p>{{ subsection.content }}</p>{% endif %}
                    {% if subsection.items %}
                    <ul class="re-list">
                        {% for item in subsection.items %}<li>{{ item }}</li>{% endfor %}
                    </ul>
                    {% endif %}
                </div>
                {% endif %}
            {% endfor %}
            {% endif %}
        </section>
        {% endfor %}

        <section class="re-faq">
            <h2>{{ page_rugby_enfants.faq.title }}</h2>
            {% for item in page_rugby_enfants.faq.questions %}
            <div class="re-faq-item">
                <h3>{{ item.question }}</h3>
                <p>{{ item.answer }}</p>
            </div>
            {% endfor %}
        </section>

        <section class="re-cta">
            <h2>{{ page_rugby_enfants.cta.title }}</h2>
            <p>{{ page_rugby_enfants.cta.subtitle }}</p>
            <div class="re-cta-buttons">
                {% for button in page_rugby_enfants.cta.buttons %}
                <a href="{{ button.url }}" class="re-btn--{{ button.style }}">{{ button.text }}</a>
                {% endfor %}
            </div>
        </section>
    </article>
</main>
