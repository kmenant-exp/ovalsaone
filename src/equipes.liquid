---
layout: layout.njk
title: "Nos Équipes Rugby - U6 à U14 | École Rugby Oval Saône Trévoux"
description: "Découvrez nos équipes de rugby pour enfants : U6, U8, U10, U12, U14. Entraîneurs qualifiés, apprentissage progressif dans un esprit familial à Trévoux."

hero:
  title: "Nos Équipes"
  subtitle: "Découvrez nos différentes catégories et leurs entraîneurs"
  
intro:
  title: "Nos équipes, notre fierté"
  text: "Plaisir, respect et camaraderie : nos équipes de jeunes de U6 à U14 grandissent ensemble autour des vraies valeurs du rugby."
---

<!-- Hero Section avec effet parallax -->
<section class="hero parallax-section subpage-hero" data-parallax-speed="0.25">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">{{ hero.title }}</h1>
        <p class="hero-subtitle">{{ hero.subtitle }}</p>
    </div>
</section>

<!-- Section Catégories -->
<section class="categories section intro">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>{{ intro.title }}</h2>
                <p>{{ intro.text }}</p>
            </div>
        </div>
        <div class="categories-grid" id="categories-grid">
            <!-- Les catégories seront chargées dynamiquement depuis JSON -->
            {% for categorie in teams %}
                <div class="equipe-card fade-in-up">
                    <div class="equipe-header">
                        <h3 class="equipe-title">{{ categorie.name }}</h3>
                        <div class="equipe-badges">
                            {% if categorie.max_age != null %}
                            <span class="equipe-age">{{ categorie.min_age }}-{{ categorie.max_age }} ans</span>
                            {% else %}
                            <span class="equipe-age">{{ categorie.min_age }} ans et +</span>
                            {% endif %}
                        </div>
                    </div>
                    <div class="equipe-content">
                        <div class="equipe-top-content">
                            <div class="equipe-info">
                                <p><strong><i class="fas fa-user-tie"></i> Entraîneur{% if categorie.coaches.size > 1 %}s{% endif %} :</strong>
                                    {% for coach in categorie.coaches %}
                                        {{ coach.name }}{% unless forloop.last %}, {% endunless %}
                                    {% endfor %}
                                </p>
                            </div>
                            <div class="equipe-description">
                                <p>{{ categorie.description }}</p>
                            </div>
                        </div>
                        <div class="equipe-planning planning-container">
                            <h4><i class="fas fa-calendar-alt"></i> Planning d'entraînement</h4>
                            <ul class="planning-list">
                                {% for seance in categorie.schedule %}
                                    <li>
                                        <strong>{{ seance.day }}</strong>
                                        <span>{{ seance.time }}</span>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>  
            {% endfor %}
        </div>
    </div>
</section>