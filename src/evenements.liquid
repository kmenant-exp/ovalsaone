---
layout: layout.njk
title: Événements - Oval Saône Rugby
description: Calendrier des événements et matchs à venir pour toutes les équipes d'Oval Saône Rugby.

hero:
  title: "Calendrier des événements"
  subtitle: "Retrouvez ici tous les matchs, entraînements et événements à venir pour nos équipes."

intro:
  title: "Ne manquez aucun rendez-vous"
  text: "Consultez notre calendrier régulièrement mis à jour pour connaître les entraînements, matchs et événements spéciaux. Vous pouvez également synchroniser le calendrier de votre équipe avec votre application préférée."
---

<!-- Hero Section avec effet parallax -->
<section class="hero parallax-section subpage-hero">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">{{ hero.title }}</h1>
        <p class="hero-subtitle">{{ hero.subtitle }}</p>
    </div>
</section>

<section class="intro section">
    <div class="container">
        <div class="intro-content">
            <div class="intro-text">
                <h2>{{ intro.title }}</h2>
                <p>{{ intro.text }}</p>
            </div>
        </div>
    </div>
</section>

<section class="calendar-section">
    <div class="container">
        <div class="section-intro">
            <div class="calendar-links">
                <p>Vous pouvez également ajouter les calendriers à votre application préférée :</p>
                <div class="calendar-links-grid">
                    {% for team in calendars.teams %}
                    <div class="calendar-links-team">
                        <h3 class="team-name">{{ team.name }}</h3>
                        <div class="calendar-links-buttons">
                            <a href="{{ team.calendarUrl }}" target="_blank" class="calendar-link google">
                                <span class="calendar-icon google-icon"></span>
                                <span class="link-text">Google Calendar</span>
                            </a>
                            <a href="{{ team.icsUrl }}" download class="calendar-link ics">
                                <span class="calendar-icon ics-icon"></span>
                                <span class="link-text">Format ICS</span>
                            </a>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="calendar-tabs">
            <ul class="tab-buttons">
                <li class="active" data-team="all">Tous les événements</li>
                {% for team in calendars.teams %}
                <li data-team="{{ team.name }}">{{ team.name }}</li>
                {% endfor %}
            </ul>
        </div>

        <div class="calendar-container">
            <div class="calendar-loading">
                <p>Chargement des événements...</p>
            </div>
            <div class="calendar-error" style="display:none;">
                <p>Impossible de charger les événements. Veuillez réessayer plus tard.</p>
            </div>
            <div class="calendar-section">
                <div class="calendar-events" style="display:none;">
                    <!-- Les événements à venir du calendrier seront chargés ici via JavaScript -->
                </div>
            </div>
            <div class="past-events-section">
                <button class="btn btn-secondary past-events-button" style="display:none;">Voir les événements passés</button>
                <div class="calendar-past-events" style="display:none;">
                    <!-- Les événements passés du calendrier seront chargés ici via JavaScript -->
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    // Nous exposons les données des calendriers pour que le JS puisse y accéder
    window.calendarConfig = {
        apiKey: "{{ calendars.apiKey }}",
        teams: [
            {% for team in calendars.teams %}
            {
                name: "{{ team.name }}",
                calendarId: "{{ team.calendarId }}"
            }{% if forloop.last == false %},{% endif %}
            {% endfor %}
        ]
    };
</script>

<script src="/js/evenements.js" defer></script>